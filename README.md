🚀 سامانه مدیریت تسک (Task Management System) - پنل فرانت‌اند
این پروژه بخش کلاینت یک اپلیکیشن مدیریت تسک است که با تمرکز بر تجربه کاربری (UX) و معماری تمیز (Clean Architecture) توسعه یافته است.

🛠 تکنولوژی‌های استفاده شده
Next.js 14 (App Router): برای مدیریت روتینگ و بهینه‌سازی رندرینگ.

TypeScript: جهت افزایش پایداری کد و جلوگیری از خطاهای زمان اجرا.

Zustand: برای مدیریت وضعیت سراسری (Global State) با قابلیت ماندگاری (Persist).

Axios: برای مدیریت درخواست‌های HTTP و استفاده از Interceptors.

Tailwind CSS: جهت طراحی رابط کاربری مدرن، واکنش‌گرا (Responsive) و زیبا.

Lucide React: برای استفاده از آیکون‌های بهینه و زیبا.

✨ ویژگی‌های کلیدی (Features)
احراز هویت کامل (Auth Flow): شامل ثبت‌نام و ورود کاربر با مدیریت توکن‌های JWT.

مدیریت تسک‌ها (CRUD): قابلیت مشاهده، ایجاد، ویرایش و حذف تسک‌ها به صورت تعاملی.

فیلترینگ هوشمند: امکان فیلتر کردن تسک‌ها بر اساس وضعیت (انجام شده / در حال انجام).

امنیت سطح بالا:

استفاده از Axios Interceptors برای تزریق خودکار توکن به هدرها.

پیاده‌سازی Middleware برای محافظت از مسیرهای داخلی (Protected Routes).

معماری لایه‌بندی شده: تفکیک کامل لایه‌های UI، Hooks، Services و Store.

🏗 ساختار پروژه (Folder Structure)
Plaintext
src/
├── app/            # صفحات اصلی و پیکربندی Next.js
├── components/     # اجزای رابط کاربری (UI Components)
├── hooks/          # هوک‌های اختصاصی برای مدیریت منطق (Business Logic)
├── services/       # لایه ارتباط با API (Axios Instance & Requests)
├── store/          # مدیریت وضعیت سراسری با Zustand
├── types/          # اینترفیس‌ها و تایپ‌های TypeScript
└── middleware.ts   # محافظت از دسترسی‌های غیرمجاز
⚙️ راه‌اندازی پروژه
۱. نصب وابستگی‌ها:

Bash
npm install
۲. تنظیم فایل .env.local:

Code snippet
NEXT_PUBLIC_API_URL=your_backend_api_url
۳. اجرای پروژه در حالت توسعه:

Bash
npm run dev
📝 یادداشت‌های فنی (برای دمو)
State Management: به جای Context API از Zustand استفاده شده تا علاوه بر پرفورمنس بالاتر، دیتای کاربر با رفرش صفحه (از طریق LocalStorage) حفظ شود.

Separation of Concerns: منطقِ مربوط به دیتای هر بخش در Custom Hooks کپسوله شده تا کامپوننت‌ها فقط مسئول نمایش (Render) باشند.

API Handling: تمامی خطاهای سمت سرور به صورت متمرکز در لایه هوک مدیریت شده و با پیام‌های مناسب به کاربر نمایش داده می‌شوند.
